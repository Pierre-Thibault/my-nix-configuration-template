# Update with what you need
{
  hostname = "";
  system = "x86_64-linux";
  userfullname = "";
  username = "";
  mainDisk = "/dev/sda";
  opensshKeys = [
  ];
  systemStateVersion = "26.05"; # Starting version and then, only compatibility version (cannot upgrade by changing)
}
